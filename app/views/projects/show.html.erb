<h1><%= @project.title %></h1><br>

<h5>Style: <%= @project.style.camelcase %></h5><br>

<h5>Tools used:</h5><br>
    <ul>
    <% @project.projects_tools.each do |pt|%>
        <%= link_to pt.tool.name, project_projects_tool_path(@project, pt) %><br>
    <% end %>
    </ul>
<%= link_to "Add a Tool", new_project_projects_tool_path(@project) %><br>

<% if @project.photo.attached? %>
    <image class="resize" src="<%= (url_for(@project.photo)) %>">
<% end %>
<br>
<%= link_to "Edit Project", edit_user_project_path(@project.user, @project) %><br>
<%= button_to "Delete Project", user_project_path(@project.user, @project), method: :delete %>
